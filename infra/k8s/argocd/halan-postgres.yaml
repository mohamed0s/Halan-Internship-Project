apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: halan-postgres
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: postgresql
    targetRevision: "15.5.0"
    helm:
      valueFiles:
        - https://raw.githubusercontent.com/mohamed0s/Halan-Internship-Project/HEAD/infra/k8s/helm/postgres/postgres-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: halan
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
