apiVersion: batch/v1
kind: CronJob
metadata:
  name: db-backup-cronjob
  namespace: halan
spec:
  jobTemplate:
    metadata:
      name: db-backup-cronjob
    spec:
      template:
        metadata: {}
        spec:
          containers:
          - image: busybox:latest
            name: db-backup-cronjob
            resources: {}
          restartPolicy: OnFailure
  schedule: '*/5 * * * *'
status: {}
